import Info from "./Info";
import Map from "./Map";

const MapAndInfo = () => {
  return (
    <div className="p-2" style={{ backgroundColor: "#D3D3D4" }}>
      <Info />
      <Map />
    </div>
  );
};

export default MapAndInfo;
